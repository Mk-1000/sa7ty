<form method="post" action="{{ path('app_avilibility_delete', {'id': avilibility.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ avilibility.id) }}">
    <button class="btn">Delete</button>
</form>
